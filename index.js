/**
 * uploader class
 * 1. create a uploader
 * 2. vaildate the file's type, size, oth.
 * 3. call ali-oss upload fn to upload files
 */
export class Uploader {
  // start uploader and upload files
  create (sts, opt = {}, callBack) {
    console.log('create')
  }
}